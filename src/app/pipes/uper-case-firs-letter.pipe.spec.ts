import { UperCaseFirsLetterPipe } from './uper-case-firs-letter.pipe';

describe('UperCaseFirsLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new UperCaseFirsLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
